\documentclass{article}

\usepackage{a4wide}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{dsfont}
\usepackage{stmaryrd}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{cleveref}
\usetikzlibrary{shapes.geometric} % For the triangle

\newtheorem{thm}{Theorem}[section]
\newtheorem{cor}[thm]{Corollary}
\newtheorem{lem}[thm]{Lemma}
\newtheorem{prop}[thm]{Proposition}
\newtheorem{defi}[thm]{Definition}
\newtheorem{remark}[thm]{Remark}
\newtheorem{example}[thm]{Example}

\newcommand*{\defeq}{\stackrel{\textrm{def}}{=}}

\newcommand*{\lfalse}{\mathbf{false}}
\newcommand*{\ltrue}{\mathbf{true}}

\newcommand*{\sfalse}{\textit{false}}
\newcommand*{\strue}{\textit{true}}

\newcommand*{\db}[1]{\left\llbracket #1 \right\rrbracket}

\newcommand*{\Expr}{\mathsf{Expr}}
\newcommand*{\Assn}{\mathsf{Assn}}

\newcommand*{\FinMea}{\mathrm{FinMea}}
\newcommand*{\support}{\mathrm{supp}}


\newcommand{\cmt}[3]{{{\color{#3}{\bf [}{\bf #1:} {\it #2}{\bf ]}}}}
\newcommand{\gj}[1]{\cmt{GJ}{#1}{blue}}
\newcommand{\hy}[1]{\cmt{HY}{#1}{red}}
\newcommand{\shp}[1]{\cmt{SP}{#1}{purple}}


% --- Common Style Settings ---
\tikzset{
    inline/.style={scale=0.15},
    vertex/.style={circle, fill=black, inner sep=0pt, minimum size=3pt}
}

% 1. Three vertices, Zero edges (Independent Set / Empty Graph)
\newcommand{\Ithree}{%
  \begin{tikzpicture}[inline]
    \coordinate (a) at (90:1);
    \coordinate (b) at (210:1);
    \coordinate (c) at (330:1);
    \node[vertex] at (a) {};
    \node[vertex] at (b) {};
    \node[vertex] at (c) {};
  \end{tikzpicture}%
}

% 2. Three vertices, One edge
\newcommand{\Ethree}{%
  \begin{tikzpicture}[inline]
    \coordinate (a) at (90:1);
    \coordinate (b) at (210:1);
    \coordinate (c) at (330:1);
    \draw (b) -- (c); % The single edge
    \node[vertex] at (a) {};
    \node[vertex] at (b) {};
    \node[vertex] at (c) {};
  \end{tikzpicture}%
}

% 3. Three vertices, Two edges (Path P3 or Cherry)
\newcommand{\Pthree}{%
  \begin{tikzpicture}[inline]
    \coordinate (a) at (90:1);
    \coordinate (b) at (210:1);
    \coordinate (c) at (330:1);
    \draw (b) -- (a) -- (c); % The two edges connecting at the top
    \node[vertex] at (a) {};
    \node[vertex] at (b) {};
    \node[vertex] at (c) {};
  \end{tikzpicture}%
}

% Define a command for a small Triangle (K3)
\newcommand{\Kthree}{%
  \begin{tikzpicture}[inline]
    \coordinate (a) at (90:1);
    \coordinate (b) at (210:1);
    \coordinate (c) at (330:1);
    \draw (b) -- (a) -- (c) -- (b); % The two edges connecting at the top
    \node[vertex] at (a) {};
    \node[vertex] at (b) {};
    \node[vertex] at (c) {};
  \end{tikzpicture}%
}


\title{Yet Another Survey on Razborov's Flag Algebra}
\author{Gyeongon Jung \and Seonghun Park \and Hongseok Yang}
\date{\today}

\begin{document}
\maketitle

\section{Introduction}

\hy{Space filler. To be expanded later. Make sure that we do not assume too much background knowledge on extremal graph theory. Also showcasing the use of flag algebra in solving some problem in extremal graph theory would be useful.}

Razborov's flag algebra \cite{Razborov2007} is a powerful framework for
reasoning about asymptotic densities in combinatorial structures, especially
in extremal graph theory. Since its introduction, it has been successfully
applied to solve several longstanding open problems, such as the minimum triangle density
problem \cite{Razborov2008} and the minimum pentagon density problem \hy{Citation}. Moreover, the flag algebra framework has been
implemented in computer programs, enabling researchers to obtain numerical
bounds for various extremal problems, some of which even resolved open conjectures \hy{Citation}.

This is a survey on Razborov's flag algebra targetted at
readers with a background in theoretical computer science, particularly those
interested in programming languages and formal methods. While most articles on flag algebras emphasise
the algebraic aspect of the flag algebra framework, we focus on its logic aspect and present the syntax, semantics, and reasoning principles of the framework in a manner similar to formal logic systems.

Before starting, we fix some notation and terminology used throughout the paper.
For each $n \in \mathbb{N}$, we write $[n]$ for the set $\{1, 2, \ldots, n\}$. For a set $X$ and a nonnegative integer $n$, we write $\binom{X}{2}$ to mean the set of all 2-element subsets of $X$. Throughout the paper, we are concerned with finite simple undirected graphs $G$ whose vertex set is a subset of $\mathbb{N}$, and call them simply as graphs. We use $\mathcal{G}$ to mean the set of all such graphs. For a graph $G$, we let $V(G)$ and $E(G)$ be the vertex set and the edge set of $G$, respectively, and write $v(G)$ and $e(G)$ for their cardinalities. For a set $U \subseteq V(G)$, we define $G[U]$ to be the subgraph of $G$ induced by $U$. We use the standard notation for well-known graphs. We write $K_n$ for the complete graph with the vertex set $[n]$, and $K_{n,m}$ for the complete bipartite graph that has the vertex partition $(\{1,2,\ldots,n\},\{n+1,\ldots,n+m\})$. The $C_n$ means the cycle graph with $n$ vertices, i.e., $V(C_n) = [n]$ and $E(C_n) = \{\{i,i+1\} \mid i \in [n-1]\} \cup \{\{1,n\}\}$, and the $P_n$ is the path graph with $n$ vertices, i.e., $V(P_n) = [n]$ and $E(P_n) = \{\{i,i+1\} \mid i \in [n-1]\}$.

\section{Induced Subgraph Density, Limiting Graphs, and Extremal Problems}

Let $H$ and $G$ be graphs. The most important concept in flag algebra is the \emph{induced subgraph density} of $H$ in $G$ or simply \emph{$H$-density} in $G$. It is defined as
\[
p(H, G) \defeq \Pr(G[\mathbf{U}] \simeq H)
\]
where $\mathbf{U}$ is a uniformly random subset of $V(G)$ with size $v(H)$, and $\simeq$ denotes the graph isomorphism. For instance, if $H = K_2$ and $G = K_3$, then $p(H,G) = 1$ since every induced subgraph of $G$ with two vertices is isomorphic to $H$. As another example, if $H = K_2$ and $G = P_3$, then $p(H,G) = \frac{2}{3}$ since there are three induced subgraphs of $G$ with two vertices, two of which are isomorphic to $H$.

In flag algebra, we analyse the properties of graphs using the density function $p$. The target graph $G$ of this analysis goes to the second parameter of $p$, which we call 
\emph{host graph}. The first parameter $H$ of $p$, on the other hand, is a part of property speficiation, and describes a pattern to be searched for in $G$. We call $H$ \emph{pattern graph}. Usually, $G$ is large, $H$ is small, and $p(H,G)$ describes a local property of $G$.

Note that the density function $p$ induces the representation of a graph $G$ as an infinite-dimensional vector in $[0,1]^{\mathcal{G}}$ indexed by all graphs in $\mathcal{G}$. This representation is injective up to graph isomorphism, because $p(\_,G) = p(\_,G')$ is equivalent to $G \simeq G'$. It also lets us use the product topology on $[0,1]^{\mathcal{G}}$ to reason about asymptotic properties of graph sequences, as we explain next.

Let $(G_n)_{n \in \mathbb{N}}$ be a sequence of graphs. The sequence is \emph{increasing} if $v(G_n) < v(G_{n+1})$ for all $n \in \mathbb{N}$. It is \emph{convergent} if the sequence is increasing and $\lim_{n \to \infty} p(H, G_n)$ exists for every graph $H$. Note that the latter condition means that the sequence of vector representations $(p(\_,G_n))_n$ of $(G_n)_n$ converges pointwise 
in $[0,1]^{\mathcal{G}}$. The reader might be worried that this definition of convergence is too strong, as it requires the existence of the limit for every graph $H$, so that only a handful of increasing graph sequences converge. We reassure such a reader that convergence actually occurs commonly; every increasing sequence of graphs has a convergent subsequence. The reason for this is that the pointwise convergence of vector representations of graphs comes from the product topology on $[0,1]^{\mathcal{G}}$ that is compact by Tychonoff's theorem.

We call a function $\phi$ from the set of all graphs to $[0,1]$ a \emph{limiting graph} if there exists a convergent sequence of graphs $(G_n)_{n \in \mathbb{N}}$ such that $\phi(H) = \lim_{n \to \infty} p(H, G_n)$ for every graph $H$. We write 
$\Phi$ for the set of all limiting graphs. A good mental picture (which can be formalised in terms of graphons) is that a limiting graph $\phi$ is the infinite-dimensional vector representing an undirected graph with a continuum number of vertices, where the component of $\phi$ at a pattern graph $H$ is the density of $H$ in this infinitely-large host graph. We point out that every convergent sequence $(G_n)_n$ induces a unique limiting graph $\phi$, but a limiting graph $\phi$ can be induced by multiple convergent sequences.

Using limiting graphs, we can express asymptotic extremal graph theory problems in a clean manner. A representative example is an asymptotic and induced-graph version of the Tur\'an-type problem where we want to determine the maximum possible density of a graph $H$ in large host graphs that avoid certain forbidden graphs in $\mathcal{F}$. More formally, for a graph $H$ and a finite set of graphs $\mathcal{F}$, the problem asks for the solution of the following asymptotic optimisation problem:
\[
T(H, \mathcal{F}) \defeq
\lim_{n \to \infty} \max \{ p(H, G) \,\mid\, v(G) = n \text{ and } p(F,G) = 0 \text{ for all } F \in \mathcal{F} \}.
\]
The solution $T(H,\mathcal{F})$ can be reformulated in terms of limiting graphs as follows:
\[
T(H, \mathcal{F}) =
\max \{ \phi(H) \,\mid\, \phi \in \Phi \text{ and } \phi(F) = 0 \text{ for all } F \in \mathcal{F} \}.
\]
When $H = K_2$ and $\mathcal{F} = \{K_3\}$, the answer is known to $1/2$ by the famous Mantel's theorem \hy{Citation}. More generally, when $H = K_2$ and $\mathcal{F} = \{K_{r+1}\}$ for an integer $r \geq 2$, the answer is known to be $1 - \frac{1}{r}$ by the well-known Tur\'an's theorem \hy{Citation}. 
Another example is the Ramsey multiplicity problem, which is a counting version of the famous problem on Ramsey numbers $R(s,t)$. For integers $s,t \geq 2$, the problem is to answer the next asymptotic optimisation problem: 
\[
R(s,t) \defeq
\lim_{n \to \infty} \min \{ p(K_s, G) + p(K_t, \overline{G}) \,\mid\, v(G) = n \},
\]
where $\overline{G}$ is the complement graph of $G$ (i.e., $V(\overline(G)) = V(G)$ and $E(\overline{G}) = \binom{V(G)}{2} \setminus E(G)$). Using limiting graphs, we can reformulate this problem as the next optimising problem over limiting graphs:
\[
R(s,t) =
\min \{ \phi(K_s) + \phi(\overline{K_t}) \,\mid\, \phi \in \Phi \}.
\]

A main message of this survey is that the flag algebra framework is a logic for reasoning about limiting graphs in $\Phi$, in particular, the one designed for answering questions on asymptotic extremal problems. For instance, the framework provides a syntax for expressing  bounds 
\[
T(H, \mathcal{F}) \leq c
\qquad\text{and}\qquad
R(s,t) \geq c'
\]
for some $c,c' \in \mathbb{R}$ (which require reasoning about all limiting graphs in $\Phi$),
and rules for proving the above bounds, some of which can even be automated.
In the next two sections, we elaborate this message by presenting the syntax and semantics of this logic, and the reasoning principles it supports.

\section{Syntax and Semantics of Flag Algebra}
The syntax of flag algebra consists of \emph{density expressions} $E$ and \emph{assertions} $A$. Density expressions generalise pattern graphs $H$, and define maps from limiting graphs to real numbers. Such maps correspond to features in machine learning, and describe properties of limiting graphs quantitatively. Assertions express qualitiative properties of limiting graphs using density expressions and logical connectives.

Formally, the syntax is defined as follows:
\begin{align*}
\text{Density Expressions}\quad E & ::= H \,\mid\, r \cdot E \,\mid\, 0 \,\mid\, E + E \,\mid\, 1 \,\mid\, E \cdot E
\\
\text{Assertions}\quad A & ::= \lfalse \,\mid\, \ltrue \,\mid\, E \geq E \,\mid\, \neg A \,\mid\, A \lor A 
\end{align*}
where $H$ is a graph, and $r \in \mathbb{R}$. We denote the set of all density expressions by $\Expr$, and that of assertions by $\Assn$.  Note that density expressions are built from pattern graphs using real scalar multiplication, addition, multiplication, and the constants $0$ and $1$. Assertions are constructed from the boolean constants $\lfalse$ and $\ltrue$, comparisons between density expressions, negation, and disjunction. The negation and disjunction here have the usual meanings from classical logic. As a result, they induce other logical connectives of classical logic such as conjunction and implication in the standard way:
\[
A \land A' \defeq \neg (\neg A \lor \neg A')
\qquad\text{and}\qquad
A \implies A' \defeq \neg A \lor A'.
\]
We also note that usual comparison operators between density expressions such as $=$, $>$, $\leq$, and $<$ can be defined using $\geq$ and the logical connectives in the standard way. For instance,
\[
(E_1 = E_1) \defeq (E_1 \geq E_2) \land (E_2 \geq E_1)
\quad\text{and}\quad
(E_1 < E_2) \defeq \neg (E_1 \geq E_2).
\]
Finally, we write $-E$ for $(-1) \cdot E$ and $E_1 - E_2$ for $E_1 + (-E_2)$.

The formal semantics of density expressions $E$ and assertions $A$ has the form:
\[
\db{E} : \Phi \to \mathbb{R} 
\qquad\text{and}\qquad
\db{A} : \Phi \to \mathbb{B}
\]
where $\mathbb{B}$ is the set of boolean values $\{\strue,\sfalse\}$.
It is defined by induction on the structure of $E$ and $A$ using the ordered commutative algebra structure of $\mathbb{R}$ and the boolean algebra structure of $\mathbb{B}$: for a limiting graph $\phi \in \Phi$,
\begin{align*}
\db{H}\phi & \defeq \phi(H),
&
\db{r \cdot E}\phi & \defeq r \cdot
\db{E}\phi,
\\
\db{0}\phi & \defeq 0,
&
\db{E_1 + E_2}\phi & \defeq
\db{E_1}\phi + \db{E_2}\phi,
\\
\db{1}\phi & \defeq 1,
&
\db{E_1 \cdot E_2}\phi & \defeq
\db{E_1}\phi \cdot \db{E_2}\phi,
\end{align*}
and
\begin{align*}
\db{\lfalse}\phi & \defeq \sfalse,
&
\db{\ltrue}\phi & \defeq \strue,
\\
\db{E_1 \geq E_2}\phi & \defeq
\begin{cases}
    \strue & \text{if } \db{E_1}\phi \geq \db{E_2}\phi, 
    \\ 
    \sfalse & \text{otherwise},
\end{cases}
&
\db{\neg A}\phi & \defeq
\begin{cases}
    \strue & \text{if } \db{A}\phi = \sfalse, 
    \\ 
    \sfalse & \text{otherwise},\end{cases}
\\
\db{A_1 \lor A_2}\phi & \defeq 
\begin{cases}
    \strue & \text{if } \db{A_1}\phi = \strue \text{ or } \db{A_2}\phi = \strue, 
    \\ 
    \sfalse & \text{otherwise}.
\end{cases}
\end{align*}
We say that an assertion $A$ is \emph{valid} if $\db{A}\phi = \strue$ for every limiting graph $\phi \in \Phi$,
and that it is \emph{satisfiable} if $\db{A}\phi = \strue$ for some limiting graph $\phi \in \Phi$.

Note that the syntax and semantics of flag algebra are just those of arithmetic expressions and assertions (or formulas) in the quantifier-free fragment of standard first-order logic. Graphs $H$ in density expressions correspond to real-valued variables, density expressions to arithmetic expressions over these variables, and assertions to logical formulas over these arithmetic expressions. As a result, many reasoning principles from first-order logic carry over to flag algebra directly.
For instance, the following assertions hold for all limiting graphs $\phi \in \Phi$:
\begin{align*}
  & H_1 \cdot (H_3 + H_2) + (4 \cdot H_1) \cdot H_3 = H_1 \cdot H_2 + 5 \cdot (H_1 \cdot H_3), 
  \\
  & (H_1 + H_2 \geq 0) \wedge (0 \geq H_2 - H_3) \implies H_1 + H_3 \geq 0.
\end{align*}

\begin{lem}
  \label{lem:basic-algebra}
    The rules of ordered commutative algebras form valid equations for density expressions. Also, the rules for boolean algebras are valid assertions in flag algebra.
\end{lem}

What is then special about flag algebra in comparison with first-order logic? The answer comes from the fact that variables (i.e., graphs $H$) in flag algebra cannot be set arbitrarily, but their values must come from have a limiting graph. Because of this fact, two different variables representing isomorphic graphs always have the same value, and each variable has a value in $[0,1]$. Also, the sum of a collection of variables $\mathcal{H}_n$ is semantically equal to the constant density expression $1$, if those variables represent all the graphs on the vertex set $[n]$ uniquely up to isomorphism. Furthermore, any variable representing a graph $G$ with $v(G) \leq n$ is equal to a linear combination of the variables in $\mathcal{H}_n$. We formalise these observations in the next lemmas. 
\begin{lem}
    If graphs $H$ and $H'$ are isomorphic, the equation $H = H'$ is valid.
\end{lem}
\begin{lem}
  \label{lem:non-negativity}
    The inequality $H \geq 0$ is valid for every graph $H$.
\end{lem}
\begin{lem}
  \label{lem:decomposition}
    Consider a non-negative integer $n$. Let $\mathcal{H}_n$ be the set of graphs 
    on the vertex set $[n]$ such that every graph on the vertex set $[n]$ is isomorphic to exactly one graph in $\mathcal{H}_n$. Then, the equation 
    \[
    \sum_{H \in \mathcal{H}_n} H = 1
    \] 
    is valid. Also, for every graph $G$ with $v(G) \leq n$, there exist real numbers $(r_H)_{H \in \mathcal{H}_n}$ such that the equation
    \[
    G = \sum_{H \in \mathcal{H}_n} r_H \cdot H
    \]
    is valid.
\end{lem}

Using flag algebra, we can express asymptotic extremal problems mentioned in the previous section. The 
Tur\'an-type problem for a graph $H$ and a finite set of forbidden graphs $\mathcal{F}$ is to find a tightest $c \in \mathbb{R}$ such that the implication
\begin{equation}
  \label{eqn:turan-assertion}
\left(\bigwedge_{F \in \mathcal{F}} F = 0 \right) \implies H \leq c.
\end{equation}
holds for all limiting graphs $\phi$ in flag algebra. The condition $\bigwedge_{F \in \mathcal{F}} F = 0$ here expresses that a limiting graph $\phi$ avoids all the forbidden graphs in $\mathcal{F}$, and the conclusion $H \leq c$ states that the density of $H$ in $\phi$ is at most $c$.
Regarding the Ramsey multiplicity problem, we are given integers $s,t \geq 2$, and asked to find a tightest $c' \in \mathbb{R}$ that makes the following inequality valid:
\begin{equation}
  \label{eqn:ramsey-assertion}
I_s + K_t \geq c'
\end{equation}
We remind the reader that $I_s$ here is the edgeless graph with $s$ vertices.

\hy{Include other examples. Ideally, we want examples that showcase the expressiveness of the logic, e.g., involving complex density expressions or assertions with multiple logical connectives.}

How does flag algebra help us in solving the problems of finding tightest $c$ and $c'$ in \Cref{eqn:turan-assertion,eqn:ramsey-assertion}? The answer lies in reasoning principles of flag algebra that allow us to derive valid assertions. We give a glimpse of this aspect of flag algebra with two examples here and elaborate it in the next section.
Consider the Tur\'an-type problem with $H = K_2$ and $\mathcal{F} = \{K_3\}$, which intuitively asks us to find the best upper bound on the asymptotic edge density of triangle-free graphs. In terms of flag algebra, the problem asks us to find a tightest $c$ that makes the below implication hold for all limiting graphs:
\[
K_3 = 0 \implies K_2 \leq c
\]
The tightest $c$ is $1/2$, a well-known result called Mantel's theorem \hy{Citation}. We prove the validity of the above implication for $c = 1/2$. We start by noting the validity of the following two assertions:
\begin{align}
  K_2 & = 0 \cdot \Ithree + \tfrac{1}{3} \cdot \Ethree + \tfrac{2}{3} \cdot \Pthree + 1 \cdot \Kthree,
  \label{eqn:mantel-density-decomposition}
  \\[2ex]
  0 & \leq 1 \cdot \Ithree - \tfrac{1}{3} \cdot \Ethree - \tfrac{1}{3} \cdot \Pthree + 1 \cdot \Kthree.
  \label{eqn:mantel-key-inequality}
\end{align}
The first assertion is a consequence of \Cref{lem:decomposition}, and the second can be proved using reasoning principles of flag algebra explained in the next section. Using these assertions and the assumption $K_3 = 0$, we derive the upper bound $1/2$ on $K_2$. The derivation is given below, where each step holds for all limiting graphs $\phi \in \Phi$ satisfying $\phi(K_3) = 0$:
\begin{align*}
  K_2 
  & = 0 \cdot \Ithree + \tfrac{1}{3} \cdot \Ethree + \tfrac{2}{3} \cdot \Pthree + 1 \cdot \Kthree
  && \text{by \Cref{eqn:mantel-density-decomposition}}
  \\
  & \leq \left(0 \cdot \Ithree + \tfrac{1}{3} \cdot \Ethree + \tfrac{2}{3} \cdot \Pthree + 1 \cdot \Kthree \right) 
  + 
  \left(
  \tfrac{1}{2} \cdot \Ithree - \tfrac{1}{6} \cdot \Ethree - \tfrac{1}{6} \cdot \Pthree
+ \tfrac{1}{2} \cdot \Kthree\right)
  && \text{by \Cref{eqn:mantel-key-inequality}}
  \\
  & = \tfrac{1}{2} \cdot \Ithree + \tfrac{1}{6} \cdot \Ethree + \tfrac{1}{2} \cdot \Pthree + \tfrac{3}{2} \cdot \Kthree
  && \text{by \Cref{lem:basic-algebra}}
  \\
  & = \tfrac{1}{2} \cdot \Ithree + \tfrac{1}{6} \cdot \Ethree + \tfrac{1}{2} \cdot \Pthree  + \tfrac{1}{2} \cdot \Kthree
  && \text{since } K_3 = 0 
  \\
  & \leq \tfrac{1}{2} \cdot \Ithree + \tfrac{1}{2} \cdot \Ethree + \tfrac{1}{2} \cdot \Pthree + \tfrac{1}{2} \cdot \Kthree
  && 
  \text{by \Cref{lem:non-negativity}}
  \\
  & \leq \tfrac{1}{2} \cdot \left(\Ithree + \Ethree + \Pthree + \Kthree\right)
  && 
  \text{by \Cref{lem:basic-algebra}}
  \\
  & = \tfrac{1}{2} \cdot 1 
  && \text{by \Cref{lem:decomposition}}
  \\
  & = \tfrac{1}{2}.
\end{align*}
The next example is for the Ramsey multiplicity problem with $s = t = 3$, which asks us to find a tightest $c'$ that makes the following inequality valid for all limiting graphs:
\[I_3 + K_3 \geq c'.
\]
The tightest $c'$ is known to be $1/4$ by Goodman's result \hy{Citation}. We prove the validity of the above inequality for $c' = 1/4$ as follows:
\begin{align*}
  I_3 + K_3 
  & = 1 \cdot \Ithree + 0 \cdot \Ethree + 0 \cdot \Pthree + 1 \cdot \Kthree
  && \text{by \Cref{eqn:mantel-density-decomposition}}
  \\
  & \geq \left(1 \cdot \Ithree + 0 \cdot \Ethree + 0 \cdot \Pthree + 1 \cdot \Kthree\right)
  -
  \left(
  \tfrac{3}{4} \cdot \Ithree - \tfrac{1}{4} \cdot \Ethree - \tfrac{1}{4} \cdot \Pthree + \tfrac{3}{4} \cdot \Kthree
  \right)
  && \text{by \Cref{eqn:mantel-key-inequality}}
  \\
  & = \tfrac{1}{4} \cdot \Ithree + \tfrac{1}{4} \cdot \Ethree + \tfrac{1}{4} \cdot \Pthree + \tfrac{1}{4} \cdot \Kthree
  && \text{by \Cref{lem:basic-algebra}}
  \\
  & = \tfrac{1}{4} \cdot (\Ithree + \Ethree + \Pthree + \Kthree)
  && 
  \text{by \Cref{lem:basic-algebra}}
  \\
  & = \tfrac{1}{4} \cdot 1 
  && \text{by \Cref{lem:decomposition}}
  \\
  & = \tfrac{1}{4}.
\end{align*}

\hy{Explain the notation. Real constants mean real constants multiplied by the density expression $1$. It might be better to change the order of these two examples.}

\section{Reasoning with Flag Algebra}

The previous section illustrated how to reason about limiting graphs using flag algebra with two examples. But both examples include a step not justified yet, namely, the inequality in \Cref{eqn:mantel-key-inequality} which is recalled below:
\[
0 \leq 1 \cdot \Ithree - \tfrac{1}{3} \cdot \Ethree - \tfrac{1}{3} \cdot \Pthree + 1 \cdot \Kthree.
\]
The purpose of this section is to explain a reasoning principle of flag algebra which allows us to prove such inequalities.

At a high level, the reasoning principle is based on the idea of defining a collection of variants of flag algebra and establishing, for each variant, a mechanism for transferring valid inequalities in the variant to valid inequalities in the original flag algebra. Each variant is designed to make certain inequalities easier to prove. An intended use case is to prove such easy inequalities in some variants, and transfer them to the original flag algebra using the mechanism. The variants we consider here are obtained by repeating the construction of flag algebra so far but with vertex-labelled graphs, instead of unlabelled graphs. For instance, the inequality in \Cref{eqn:mantel-key-inequality} is derived from an easily-provable inequality in a variant that considers graphs with one vertex labelled with~$1$.

To express this principle formally, we need some additional definitions.
A \emph{type} $\tau$ is a graph on the vertex set $[k]$ for some $k \in \mathbb{N}$. We call $k$ the \emph{size} of $\tau$. A \emph{$\tau$-labelled graph} is a pair $G = (G', \theta)$ of a graph $G'$ and an injective map $\theta : [k] \to V(G')$ that preserves both edges and non-edges of $\tau$. We call $\tau$ the \emph{type} of $G$, and write $V(G)$ and $E(G)$ for the vertex set and edge set of $G'$, respectively, and also $v(G)$ and $e(G)$ for their cardinalities. For a type $\tau$, we write $\mathcal{G}^\tau$ for the set of all $\tau$-labelled graphs. 

We now repeat the concepts introduced in the previous sections but for $\tau$-labelled graphs instead of unlabelled graphs.
For $\tau$-labelled graphs $H = (H',\theta)$ and $G = (G',\theta')$, a function $h : V(H') \to V(G')$ is a \emph{$\tau$-homomorphism} if it is a homomorphism from $H'$ to $G'$ and satisfies $h(\theta(i)) = \theta'(i)$ for all $i \in [k]$. We say that $H$ and $G$ are \emph{$\tau$-isomorphic}, denoted by $H \simeq_\tau G$, if there exists a bijective $\tau$-homomorphism from $H$ to $G$ such that its inverse is also a $\tau$-homomorphism from $G$ to $H$. 

For $\tau$-labelled graphs $H = (H',\theta)$ and $G = (G',\theta')$, the \emph{induced labelled-subgraph density} of $H$ in $G$ or simply \emph{$H$-density} in $G$ is defined as
\[
p_\tau(H,G) = \mathbb{P}\Big[G'\big[\theta'([k]) \cup \mathbf{U}\big] \simeq_\tau H\Big]
\]
where $\mathbf{U}$ is a uniform random subset of $V(G') \setminus \theta'([k])$ with size $v(H') - k$. A sequence of 
$\tau$-labelled graphs $(G_n)_{n \in \mathbb{N}}$ is \emph{increasing} if $v(G_n) < v(G_{n+1})$. Such a sequence
\emph{converges} if it is increasing and $\lim_{n \to \infty} p_\tau(H, G_n)$ exists for every $\tau$-labelled graph $H$. A function $\phi : \mathcal{G}^\tau \to [0,1]$ is a \emph{$\tau$-labelled limiting graph} if there exists a convergent sequence of $\tau$-labeled graphs $(G_n)_{n \in \mathbb{N}}$ such that $\phi(H) = \lim_{n \to \infty} p_\tau(H, G_n)$ for every $\tau$-labelled graph $H$. We write $\Phi^\tau$ for the set of all $\tau$-labelled limiting graphs.

The variant of flag algebra for $\tau$-labelled graphs is defined in the same way as the original flag algebra but by replacing graphs, induced subgraph densities, convergent sequences, and limiting graphs with $\tau$-labelled graphs, induced labelled-subgraph densities, convergent sequences of $\tau$-labelled graphs, and $\tau$-labelled limiting graphs, respectively. Also, its syntax consists of density expressions and assertions built from $\tau$-labelled graphs, and its semantics interprets density expressions and assertions over $\tau$-labeled limiting graphs in $\Phi^\tau$. The syntax is recalled below for the reader's convenience:
\begin{align*}
\text{Density Expressions}\quad E^\tau & ::= H^\tau \,\mid\, r \cdot E^\tau \,\mid\, 0 \,\mid\, E^\tau + E^\tau \,\mid\, 1 \,\mid\, E^\tau \times E^\tau
\\
\text{Assertions}\quad A^\tau & ::= \lfalse \,\mid\, \ltrue \,\mid\, E^\tau \geq E^\tau \,\mid\, \neg A^\tau \,\mid\, A^\tau \lor A^\tau 
\end{align*}
where $H^\tau$ is a $\tau$-labelled graph, and $r \in \mathbb{R}$. We denote the set of all density expressions in this variant by $\Expr^\tau$, and that of assertions in the variant by $\Assn^\tau$. 

Note that the original flag algebra is the special case of the $\tau$-labelled variant when $\tau$ is the empty graph on the empty vertex set. We denote the empty graph with $\emptyset$. So, $\Expr = \Expr^\emptyset$ and $\Assn = \Assn^\emptyset$.

The mechanism for transferring valid inequalities in the $\tau$-labelled variant to valid inequalities in the original flag algebra is called \emph{downward operator}. It is defined by following the general recipe for relating the variant and the original flag algebras using what we call an adjoint pair of functions. We explain this recipe next, and then instantiate it to define the downward operator. 

Consider a type $\tau$,  a function $\gamma : \mathcal{G} \to \FinMea(\mathcal{G}^\tau)$, and another function $\alpha : \mathcal{G}^\tau \to \FinMea(\mathcal{G})$, where we regard $\mathcal{G}$ and $\mathcal{G}^{\sigma}$ as measurable spaces with the discrete sigma algebra and write $\FinMea(\mathcal{G})$ and $\FinMea(\mathcal{G}^\tau)$ for the sets of finite measures on $\mathcal{G}$ and $\mathcal{G}^\tau$, respectively. Intuitively, for $G \in \mathcal{G}$ and $H^\tau \in \mathcal{G}^\tau$, $\alpha(G)$ defines an unnormalised probability distribution over $\mathcal{G}^\tau$, and $\gamma(H^\tau)$ defines an unnormalised probability distribution over $\mathcal{G}$. We say that $(\alpha,\gamma)$ is an \emph{adjoint pair} if 
for all $H^\tau \in \mathcal{G}^\tau$ and $G \in \mathcal{G}$, 
\[
p(\alpha(H^\tau), G) = p_\tau(H^\tau, \gamma(G)).
\]
where
\begin{align*}
  p(\alpha(H^\tau), G) \defeq \int_{\mathcal{G}} p(H, G) \, \Big(\alpha(H^\tau)(dH)\Big)
  \quad\text{and}\quad
  p_\tau(H^\tau, \gamma(G)) \defeq \int_{\mathcal{G}^\tau} p(H^\tau, G^\tau) \, \Big(\gamma(G)(dG^\tau)\Big).
\end{align*}

The downward operator for a type $\tau$ is derived from a particular kind of adjoint pairs $(\alpha,\gamma)$ for $\tau$ where $\alpha$ and $\gamma$ satisfy additional conditions. The condition for $\gamma$ is about the extendability of $\gamma$ to limiting graphs, and that for $\alpha$ is about the finiteness of the support of $\alpha$.
We say that the map $\gamma$ is \emph{extendable to limiting graphs} if there is a function $\gamma_\infty : \Phi \to \FinMea(\Phi^\tau)$ such that for every convergent sequence of graphs $(G_n)_{n \in \mathbb{N}}$, and every limiting graph $\phi \in \Phi$
\begin{multline*}
\lim_{n \to \infty} p(H, G_n) = \phi(H) \ \text{ for all } H \in \mathcal{G}
\\
{} \implies
\lim_{n \to \infty} p_\tau(H^\tau, \gamma(G_n)) = \int_{\Phi^\tau} \phi^\tau(H^\tau) \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
\ \text{ for all } H^\tau \in \mathcal{G}^\tau.
\end{multline*}
Here we regard $\Phi^\tau$ as a measurable space with the sigma algebra induced by the product topology on $[0,1]^{\mathcal{G}^\tau}$. We say that the map $\alpha$ has \emph{finite support} if for every $H^\tau \in \mathcal{G}^\tau$, the support set
of $\alpha(H^\tau)$, i.e.,
\[
\support(\alpha(H^\tau)) \defeq
\{ H \,\mid\, \alpha(H^\tau)(\{H\}) > 0\}
\]
is finite. This finite-support condition enables us to generalise $\alpha$ to any density multiplication-free expressions in $\Expr^\tau$ using linearity.
\begin{align*}
  \alpha^\dagger(H^\tau) & \defeq \sum_{H \in \support(\alpha(H^\tau))} \alpha(H^\tau)(\{H\}) \cdot H,
  &
  \alpha^\dagger(r \cdot E^\tau) & \defeq r \cdot \alpha^\dagger(E^\tau),
  \\
  \alpha^\dagger(0) & \defeq 0,
  &
  \alpha^\dagger(E_1^\tau + E_2^\tau) & \defeq \alpha^\dagger(E_1^\tau) + \alpha^\dagger(E_2^\tau).
\end{align*}
\begin{lem}
  \label{lem:adjoint-transfer}
    Let $(\alpha,\gamma)$ be an adjoint pair for a type $\tau$. Assume that $\gamma$ is extendable to limiting graphs and has $\gamma_\infty : \Phi \to \FinMea(\Phi^\tau)$ as a witness of this extendability. Also, assume that $\alpha(H^\tau)$
    has finite support, and let $\alpha^\dagger$ be the generalisation of $\alpha$ to multiplication-free expressions. Then, for every valid inequality $E^\tau \geq 0$ in the $\tau$-labelled variant such that $E^\tau$ is multiplication-free, the inequality $\alpha(E^\tau) \geq 0$ is valid in the original flag algebra.
\end{lem}
\begin{proof}
    Let $\phi \in \Phi$ be an arbitrary limiting graph. We need to show that $\db{\alpha^\dagger(E^\tau)}\phi \geq 0$. 
    Using structural induction on $E^\tau$, we will show that 
    \begin{equation}
      \label{eq:lem:adjoint-transfer0}
      \db{\alpha^\dagger(E^\tau)}\phi = \int_{\Phi^\tau} \db{E^\tau}\phi^\tau \,\Big(\gamma_\infty(\phi)(d\phi^\tau)\Big).
    \end{equation}
    The desired conclusion then follows from this equation because $E^\tau \geq 0$ is valid in the $\tau$-labelled variant
    and so the integrand $\db{E^\tau}\phi^\tau$ is non-negative for every $\phi^\tau \in \Phi^\tau$.

    Assume that $E^\tau$ is a $\tau$-labelled graph $H^\tau$. By the definition of the limiting graph, there exists a convergent sequence of graphs $(G_n)_n$ such that 
    \begin{equation}
      \label{eq:lem:adjoint-transfer1}
      \phi(H) = \lim_{n \to \infty} p(H, G_n)  \quad\text{for all } H \in \mathcal{G}.
    \end{equation}
    Using this, we derive the target equation \Cref{eq:lem:adjoint-transfer0} as follows:
    \begin{align*}
      & \db{\alpha^\dagger(H^\tau)}\phi 
      \\
      & \quad {} = \db{\sum_{H \in \support(\alpha(H^\tau))} \alpha(H^\tau)(\{H\}) \cdot H}\phi
      && \text{by the definition of } \alpha^\dagger
      \\
      & \quad {} = \sum_{H \in \support(\alpha(H^\tau))} \alpha(H^\tau)(\{H\}) \cdot \phi(H)
      && \text{by the definition of } \db{-}
      \\
      & \quad {} = \sum_{H \in \support(\alpha(H^\tau))} \alpha(H^\tau)(\{H\}) \cdot \lim_{n \to \infty} p(H, G_n)
      && \text{by \Cref{eq:lem:adjoint-transfer1}}
      \\
      & \quad {} = \lim_{n \to \infty} \sum_{H \in \support(\alpha(H^\tau))} \alpha(H^\tau)(\{H\}) \cdot p(H, G_n)
      && \text{by the finiteness of } \support(\alpha(H^\tau))
      \\
      & \quad {} = \lim_{n \to \infty} p(\alpha(H^\tau), G_n)
      && \text{by the definition of } p(\alpha(H^\tau), G_n)
      \\
      & \quad {} = \lim_{n \to \infty} p_\tau(H^\tau, \gamma(G_n))
      && \text{by the adjointness of } (\alpha,\gamma) \text{ and \Cref{eq:lem:adjoint-transfer1}}
      \\
      & \quad {} = \int_{\Phi^\tau} \phi^\tau(H^\tau) \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      && \text{by the extendability of } \gamma,
      \\
      & \quad {} = \int_{\Phi^\tau} \db{H^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      && \text{by the definition of } \db{-}.
    \end{align*}

    Next consider the case that $E^\tau = 0$. Then, 
    \begin{align*}
      \db{\alpha^\dagger(0)}\phi 
      = \db{0}\phi
      = 0 
      = \int_{\Phi^\tau} 0 \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      = \int_{\Phi^\tau} \db{0}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big).
    \end{align*}

    The remaining cases can be proved by induction hypothesis and the linearity of integration. We first derive the target equation for $E^\tau = r \cdot F^\tau$:
    \begin{align*}
      \db{\alpha^\dagger(r \cdot F^\tau)}\phi 
      & = \db{r \cdot \alpha^\dagger(F^\tau)}\phi
      && \text{by the definition of } \alpha^\dagger
      \\
      & = r \cdot \left(\db{\alpha^\dagger(F^\tau)}\phi\right)
      && \text{by the definition of } \db{-}
      \\
      & = r \cdot \int_{\Phi^\tau} \db{F^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      && \text{by induction hypothesis}
      \\
      & = \int_{\Phi^\tau} r \cdot \left(\db{F^\tau}\phi^\tau\right) \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      && \text{by the linearity of integration}
      \\
      & = \int_{\Phi^\tau} \db{r \cdot F^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
      && \text{by the definition of } \db{-}.
    \end{align*}
    Next, we handle the case that $E^\tau = E_1^\tau + E_2^\tau$:
    \begin{align*}
        & \db{\alpha^\dagger(E_1^\tau + E_2^\tau)}\phi 
        \\
        & \quad {} = \db{\alpha^\dagger(E_1^\tau) + \alpha^\dagger(E_2^\tau)}\phi
        && \text{by the definition of } \alpha^\dagger
        \\
        & \quad {} = \db{\alpha^\dagger(E_1^\tau)}\phi + \db{\alpha^\dagger(E_2^\tau)}\phi
        && \text{by the definition of } \db{-}
        \\
        & \quad {} = \int_{\Phi^\tau} \db{E_1^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
        + \int_{\Phi^\tau} \db{E_2^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
        && \text{by induction hypothesis}
        \\
        & \quad {} = \int_{\Phi^\tau} \left(\db{E_1^\tau}\phi^\tau + \db{E_2^\tau}\phi^\tau\right) \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
        && \text{by the linearity of integration}
        \\
        & \quad {} = \int_{\Phi^\tau} \db{E_1^\tau + E_2^\tau}\phi^\tau \, \Big(\gamma_\infty(\phi)(d\phi^\tau)\Big)
        && \text{by the definition of } \db{-}.     
    \end{align*}
\end{proof}
\hy{Sum of square. Multiplication elimination. Differentiation operation. Concrete instantiation.}

We are ready to define the downward operator for each type $\tau$. The operator is the $\alpha^\dagger$ in \Cref{lem:adjoint-transfer} for a particular adjoint pair $(\alpha_d,\gamma_d)$ for the type $\tau$. The function $\gamma_d$ in this adjoint pair is defined as follows. For a graph $G \in \mathcal{G}$, we define the measure $\gamma(G) \in \FinMea(\mathcal{G}^\tau)$ by
\[
c_G \defeq \frac{|\mathrm{Aut}(\tau)| \cdot p(\tau,G)}{v(\tau)!},
\quad 
Z_G \defeq |\{G^\tau \in \mathcal{G}^\tau \,\mid\, |G^\tau| = G\}|,
\quad
\gamma(G)(\{G^\tau\}) \defeq 
\begin{cases}
\frac{c_G}{Z_G} & \text{if } |G^\tau| = G
\\
0 & \text{otherwise}.
\end{cases}
\]
On the other hand, if there is no $\tau$-labelled graph $G^\tau$ with $|G^\tau| = G$, we define the measure $\gamma(G)$ to be the zero measure, i.e., $\gamma(G)(\{G^\tau\}) \defeq 0$ for every $G^\tau \in \mathcal{G}^\tau$. The function $\alpha_d$ in this adjoint pair is defined as follows. \hy{I am here.}

\section{Plan}
\begin{itemize}
\item Brief intro to Flag Algebras. Success stories. 
\item Why do we need another survey? More for the audience of the trace B of theory. In particular,programming-language friendly.
\item Syntax and semantics. Examples.
\item Reasoning principles. 
\item Variants. Lovasz-Szegedy's approach. Other variant. 
\item Opportunities for future research.
\end{itemize}


\bibliographystyle{plain} % Styles: plain, abbrv, alpha, unsrt
\bibliography{references}

\end{document}
