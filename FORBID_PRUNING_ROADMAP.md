# Genuine pruning for arbitrary forbidden graphs — plan & progress tracker

> **Working protocol.** At the **start** of each task, read this file. At the **end**,
> update the task's status box and append a dated entry to the Progress Log (what was
> done, key lemma/file names, anything that changed the plan). Keep it current — it is the
> single source of truth for this line of work.
>
> Status legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked.

## Goal

Make the **genuine-pruning** forbid-free pipeline work for an **arbitrary forbidden graph
F** (eventually a finite *family* of forbidden graphs), end to end:

1. The user defines F in a prescribed format, with convenience commands for common graphs
   (complete graphs, cycles, paths, …).
2. The flags are generated by **genuine pruning** — F-containing graphs are never carried
   forward — rather than by enumerate-all-then-filter.
3. This is justified by the bridge **F-free ↔ density = 0**, connecting the combinatorial
   pruning predicate to the analytic forbid framework.
4. The forbid-free generation/expansion commands run on this pruning-based core.

## Decisions (locked)

- **D1 — Induced semantics.** "F-free" = *no induced copy of F*, which is exactly
  `flagDensity₁ F (unlabel G) = 0`. For complete graphs (K_r) this coincides with the usual
  (non-induced) notion. **Non-induced forbidding of non-complete graphs is out of scope**
  for now (its RHS would not be `density₁ = 0`).
- **D2 — Forbidden graph as a computable graph.** Represent F as a `Sym2Graph m` and read
  its edges directly. This decouples the framework from the *canonical forbidden flag*
  (`Sym2Flag_r_0_0_idx`) and from the empty-typed enumeration, so a forbid no longer needs
  `generate_empty_typed_flags r` + a `_toFinFlag_eq` index lookup. (This is also what frees
  `MantelHfree` from its irreducible flag-setup.)
- **D3 — Keep multiple forbidden graphs in mind.** Build the pruned generator and its
  completeness **generic in the H-free predicate** `p : ∀ {n}, Sym2Graph n → Prop`
  (decidable, iso-invariant, monotone under vertex deletion). The single-graph case is
  `p G := ¬ inducedContains F G`; the family case is `p G := ∀ F ∈ Fs, ¬ inducedContains F G`.
  See "Multi-graph design notes" below. Do **not** hardcode a single F into the generator.

## Current state (baseline, as of creation)

- **Filter-based forbid-free framework — general, wired.** `ForbidFreeGenerator.lean`
  (`generate_forbid_free_empty_typed_flags`, `generate_forbid_free_flags`) enumerates *all*
  graphs (`genSym2GraphsDedup n`) and filters by H-freeness. Works for any forbid (K3/K4/K5
  used). Completeness `genFlagsHfree_toFinset_eq` is **already generic in the graph
  predicate `q`** — reuse this.
- **Genuine pruning — K3-only, NOT wired.** `Flags/ForbidFreePruned.lean`: `hasTri`,
  `augmentAllTriFree`, `augRepsTriFree`, `hasTri_of_eqv`, `hasTri_of_restrict`,
  `augRepsTriFree_complete` (all sorry-free). Triangle-specific. Validated to K3-free class
  counts (7/14/38 at n=4/5/6). ~1.8× faster at the graph level than the filter.
- **The bridge is unproven.** `triFree G ↔ flagDensity₁ K3.toFinFlag.2 (unlabel ⟦G⟧) = 0`
  exists only as prose in `ForbidFreePruned.lean` and `papers/Notes/forbid_free_pruning_work.md`.
- **Forbid resolution today.** `resolveForbidGraph` (DensityThmGenerator) finds `K{r}` +
  `K{r}_toFinFlag_eq` and reads the canonical forbidden flag from it; `isHfree` uses
  `sym2EmptyTypeFlagDensity₁ Sym2Flag_r_0_0_idx`. Both depend on the forbidden flag (D2 removes this).
- **Existing general containment (meta only).** `containsForbiddenSubgraph` (DensityThmGenerator)
  is an elaboration-time `Bool` on edge lists and is **non-induced**; not a Lean-level Prop on
  `Sym2Graph`. We need an induced Lean Prop (the general analogue of `hasTri`).

## Task ordering

> Strategy: prove the hard bridge on **K3 first**, wire **K3 pruning end-to-end** to validate
> the architecture (and bank the flag-free win for `MantelHfree`), then lift to arbitrary F /
> families. Steps 1 (and its lift 4) are the only real new mathematics; the rest is mechanical.

- `[x]` **0. Design decisions.** D1 (induced), D2 (graph representation), D3 (generic in
  predicate / multi-graph-ready). Done — see above.

- `[x]` **1. K3 bridge (the hard analytic step).** DONE, sorry-free, in
  `Flags/ForbidFreePruned.lean`. Proved `not_hasTri_iff_triangleDensity_eq_zero`:
  `¬ hasTri G ↔ sym2EmptyTypeFlagDensity₁ ⟦triangleGraph⟧ ⟦G⟧ = 0` (and the `≠ 0` form
  `hasTri_iff_triangleDensity_ne_zero`). Supporting lemmas: `triangleGraph` (K₃ as a
  `Sym2Graph 3`) + `triangleGraph_adj_iff`; `inducedSubgraph_coe_adj_iff` (induced adjacency
  = edge membership); `nonempty_iso_triangleGraph` (complete 3-vertex graph ≅ triangle);
  `exists_triangleIso_iff` (the heart: an induced triangle exists ↔ `hasTri`);
  `triangleCount_pos_iff` (placement count > 0 ↔ `hasTri`). The density was unfolded to
  `count / C(n,3)` and the `n < 3` denominator-zero case is handled by deriving `3 ≤ n` from
  `hasTri`. Added `import Mathlib.Tactic` + `Compute.FlagDensity` to the file.

- `[ ]` **2. Wire K3 pruning end-to-end.** Route the K3 forbid-free generator through
  `augRepsTriFree` + step-1 bridge instead of the filter; make `resolveForbidGraph`/`isHfree`
  edge-based (no canonical forbidden flag, per D2). Port `MantelHfree` to this path and build
  green. *Deliverables: ~1.8× pruning win realized; `MantelHfree` becomes truly only-forbid-free
  (drops `generate_empty_typed_flags 3` + the forbidden-flag setup).*

- `[ ]` **3. Generalize the combinatorial core (generic, multi-graph-ready).**
  - `inducedContains (F : Sym2Graph m) (G : Sym2Graph n) : Prop` + `Decidable`.
  - A **generic** pruned generator over an abstract predicate `p` with hypotheses
    `p_of_eqv` (iso-invariance) and `p_of_restrict` (vertex-deletion monotonicity), plus its
    completeness theorem — generalizing `augRepsTriFree` / `augRepsTriFree_complete`.
  - Instances: single F (`¬ inducedContains F ·`) and family (`∀ F ∈ Fs, ¬ inducedContains F ·`).
  - Validate counts (K4, C4, C5) by `native_decide`.

- `[ ]` **4. Generalize the bridge.** `inducedFree F G ↔ flagDensity₁ F (unlabel ⟦G⟧) = 0`
  for arbitrary (induced) F, reusing the step-1 proof. Family version is the conjunction
  `(∀ F ∈ Fs, …) ↔ (∀ F ∈ Fs, flagDensity₁ F … = 0)` — no new bridge math.

- `[ ]` **5. Generalize the wiring.** Forbid-free commands accept an arbitrary `F : Sym2Graph m`
  (and later a family), using the step-3 generic pruned generator + step-4 bridge. Reuse
  `genFlagsHfree_toFinset_eq`'s generic plumbing.

- `[ ]` **6. User-facing format + convenience commands.** Declare F by edge list (small DSL →
  `Sym2Graph m`); wrappers `forbid_complete_graph r`, `forbid_cycle k`, `forbid_path k`, …; and
  a way to pass a *list* of forbidden graphs.

- `[ ]` **7. Migrate examples + docs.** Port `MantelHfree` (and other forbid examples) to the
  pruning-based, flag-free pipeline; update `papers/Notes/forbid_free_*`; decide whether to
  retire or keep the filter path as a fallback.

## Multi-graph design notes (for D3)

- **Predicate.** Family H-free = `fun G => ∀ F ∈ Fs, ¬ inducedContains F G`. Iso-invariant and
  restrict-monotone because a finite `∀`/`∃` of iso-invariant/monotone predicates is.
- **Generic pruned generator.** Parameterize by the predicate (not by F), exactly as
  `genFlagsHfree_toFinset_eq` is parameterized by `q`. Single F and families are instances.
- **Bridge.** Conjunction of per-F bridges; or use `∑_{F ∈ Fs} flagDensity₁ F G = 0` (densities
  are ≥ 0, so the sum is 0 iff each is 0) if a single scalar predicate is more convenient
  downstream.
- **Representation.** Allow `Fs : List (Σ m, Sym2Graph m)` (graphs of possibly different sizes).
- **Pitfall.** A family at *different* sizes means the per-F density lemmas have different flag
  sizes — keep the generator's predicate purely combinatorial (on `Sym2Graph`), and only invoke
  the bridge at the wiring layer.

## Progress log

- **2026-06-19** — Created this tracker. Decisions D1–D3 locked (induced semantics; graph-based
  forbid representation; generic-in-predicate for future multi-graph support). No code changes
  yet; next up is Task 1 (K3 bridge).
- **2026-06-19** — **Task 1 DONE** (sorry-free, builds). Proved the K3 bridge in
  `Flags/ForbidFreePruned.lean`: `not_hasTri_iff_triangleDensity_eq_zero` /
  `hasTri_iff_triangleDensity_ne_zero`, plus the heart `exists_triangleIso_iff` and helpers
  (see Task 1 above). Approach: density `= count / C(n,3)`; `count > 0 ↔ hasTri` via an induced
  triangle ⟷ 3 pairwise-adjacent vertices argument (constructing/destructing a graph iso to a
  3-element complete graph); denominator-zero (`n < 3`) handled by `hasTri → 3 ≤ n`. Fiddly
  bits resolved: `fin_cases`/`omega` needed `import Mathlib.Tactic`; the `↥(Subgraph.verts)`
  subtype has no auto `Fintype`, so the iso's vertex bijection was built explicitly with
  `Finset.equivFin` (avoiding `Fintype.card` instance friction). **Next: Task 2** — wire the
  pruned generator in. Note for Task 2: the bridge is stated for `⟦triangleGraph⟧`; identify it
  with the framework's `Sym2Flag_3_0_0_3` via `Quotient.sound` (both are the triangle), and
  show `triFreeB = isHfreeGraph` as `Bool` predicates from the bridge.
